.claude/HANDOFF.md
.netlify/
node_modules/
.DS_Store
*.log
__pycache__/
*.py[cod]
test-*.pdf
generated-uscis-api-tests/
qa-reports/
USCIS forms/
USCIS forms printed/
USCIS forms Edge print/
USCIS forms flattened/
generated-filled/
USCIS forms decrypted/
payloads/*-generated-payload.json
portal-backend/.venv/
portal-backend/.env
portal-backend/db.sqlite3
portal-backend/media/
portal-backend/staticfiles/

# Local backups & archives (never commit)
.backup-patches/
.netlify-deploy-archive/
*.bak
deno.lock

# QA fixtures / generated artifacts
answers/
payloads/

# --- Local county forms: build cache (redundant; runtime serves from public/ca-local-templates) ---
assets/form-cache/ca-local-court/

# Owner working reference docs (not site assets)
NOW Green Card Renewal Application FLOW.docx
NOW Green Card Renewal Application FLOW.pdf

# Pitch deck + build artifacts (not part of the app)
decks/

# Local county templates now live in Netlify Blobs (store: ca-local-templates),
# uploaded by scripts/upload-ca-local-templates-to-blobs.js. Not in git — too
# large (1.4GB) and served from Blobs at runtime.
astro-site/public/ca-local-templates/

# macOS Finder/iCloud duplicate-file artifacts ("name 2.ext", "name 3.ext").
# These are copies of real files created by sync/duplicate operations — never
# real work. Verified 0 tracked files match this pattern.
* 2.*
* 3.*
* 4.*
